export const enum PlayerMovementAction {
  IDLE = 'idle',
  RUN = 'run',
}

export enum PlayerDirection {
  UP = 'up',
  DOWN = 'down',
  LEFT = 'left',
  RIGHT = 'right',
}

export enum PlayerKeyAction {
  JUMP = 'space',
}
